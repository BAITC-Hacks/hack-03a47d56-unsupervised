# CLI uses environment variables. The local web server also reads
# OPENAI_API_KEY from .env in the project root; Git ignores that file.
# OPENAI_EMBEDDING_MODEL is read from the process environment only.
OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
